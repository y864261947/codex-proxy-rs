# 网关改造实施记录

2026-09-10：用户授权按讨论方案开始开发。工作分支：`feat/gateway-operations`。

产品范围以 [菜单与并发方案](navigation-and-scope-proposal.md)、[模型管理](model-management-candidates.md)、[监控](monitoring-proposal.md) 为准。候选总表不是全量实施承诺，注册、支付、套餐、返佣及多副本仍暂缓。

## 提交构建检查点（2026-09-11，Asia/Shanghai）

用户授权“先提交构建看看”。本次提交包含第二批架构依赖修正，以及第三批只读目录、手动发现和最新成功记录持久化切片，推送现有工作分支并通过草稿 PR 触发 Linux CI。以下未提交记录保留为历史状态；本检查点不代表 CI 已通过，也不授权合并、发布或生产迁移。

- `187cd75` 首轮 CI 34589178397：后端编译、clippy、迁移冻结检查、应用架构测试及 Admin 105 项测试通过；API 277 项通过、6 项失败，后续 crate 测试尚未执行。4 项新增 HTTP 测试遗漏必要的请求 ID，鉴权后提前返回 500；另 2 项为新增目录模块未登记到 API 文件清单。
- 补齐上述测试请求的 `x-request-id`，将目录源码与对应测试加入既有精确清单，不修改生产鉴权规则或放宽架构断言。修正随后续提交重跑 CI，最终结果以对应运行记录为准。

## 最新成功发现持久化切片（2026-09-11，Asia/Shanghai）

- 渠道手动发现现已保存每渠道最新成功记录；重新打开窗口或刷新页面自动从本地恢复，不自动请求上游。无记录、成功空目录、读取失败与旧连接版本分别展示；旧版本只能查看，不能追加到草稿。
- 新增 `GET /api/admin/channels/model-discovery`；POST 成功须完成观测持久化。数据库在查询前分配序号，保存时锁定渠道行并校验连接版本及已保存序号，较早查询不能覆盖较新成功结果。上游失败或保存事务回滚保留原记录，结果不确定可重新读取确认。
- 新增冻结迁移 `0011_channel_model_discovery.sql` 与校验和，旧迁移未修改。观测独立于渠道配置，不增加配置版本、不写配置变更审计、不发布运行快照；草稿采用仍需原渠道保存链。仅保留一个成功记录，删除渠道级联删除。
- 新增 Admin 数据校验/失败保留、API 本地只读/鉴权/大整数序号与 PostgreSQL 持久化/并发排序/版本变化/保存失败回滚回归。Windows 缺少 MSVC `link.exe`，后端测试、clippy 与迁移应用验证仍待 Linux CI；不能将新增测试记为已通过。
- 前端 lint、类型检查、生产构建、Rust 格式检查与 `git diff --check` 通过；11 个迁移按仓库规范化字节核验校验和通过。Windows 旧迁移工作区为 CRLF，校验使用未修改的 Git 原始字节，未重写旧文件或冻结值。
- 隔离无头 Edge 验证跨窗口/刷新恢复、显式上游查询、失败保留与重新读取恢复、旧版本禁止添加、空目录、大整数序号和无配置写入，无浏览器脚本错误；截图检查通过。模拟接口验证不替代真实后端集成测试。
- 当前代码未提交、未推送、未部署；未应用迁移、请求生产上游或修改生产数据。第三批仍未完成：多版本发现历史、定时同步、分页支持、参考价格版本及指定来源/真实 Key 测试工作台待开发。

下一步：经授权提交/推送后运行 Linux CI，优先处理后端及迁移门禁的实际失败，再推进第三批剩余功能。

## 渠道手动发现切片历史记录（2026-09-11，Asia/Shanghai）

- 在只读目录基础上继续实现 `openai_api` 已保存渠道的手动模型发现、差异预览和选择添加草稿。入口在渠道编辑窗口；最终保存复用已有版本检查、审计事务与运行快照发布，不自动删除旧模型或授权分组。
- 新增 `POST /api/admin/channels/discover-models`；查询前后检查连接版本。Provider 仅请求模型列表，限制 15 秒、2 MiB、1000 项，拒绝重定向、非法/重复 ID、未知顶层结构和未完成分页。未知能力与价格不推断为支持或免费，不执行真实生成测试。
- 新增 Provider 发现协议/错误/重定向/空目录回归、Admin 只读差异/版本竞争回归和 API 鉴权/参数回归。Windows 缺少 MSVC 链接器，后端测试及 clippy 仍待 Linux CI；架构依赖修正也尚未取得新 CI 结论。
- 本次预览是当前编辑窗口内的临时结果，不是持久化发现快照；发现历史、分页支持、定时同步、跨会话最后成功快照、参考价格版本和测试工作台仍待后续切片。第三批尚未完成，第四至第六批未开始。
- 无新增迁移、生产请求或生产数据变更；当前代码未提交、未推送、未发布。
- 本轮前端 lint、类型检查（构建内）、生产构建通过。无头 Edge 使用隔离模拟接口验证显式查询、大整数版本、差异展示、鼠标/键盘勾选、添加草稿不落库、失败保留/重试、连接更改禁用发现、取消丢弃草稿、保存失败保留输入、保存成功及版本冲突，无浏览器脚本错误；截图检查通过。这里的保存验证只针对前端与模拟接口，真实后端仍待 CI。
- 补充隔离界面边界验证：关闭窗口使迟到查询结果失效；空发现保留旧模型；1000 项上限阻止溢出并允许恰好达到上限。Rust 格式检查与 `git diff --check` 通过，未把格式检查记作后端编译/测试通过。

## 模型管理首个切片历史记录（2026-09-11，Asia/Shanghai）

- 用户授权推送后，第二批提交 `94bfac5` 已推送；CI 34576658003 最终失败：容器、前端、工作流检查成功，后端架构测试发现 `policy/access.rs` 依赖 `routing`。不能标记第二批完整验收通过。
- 本轮将来源/号池身份归属到 Core identity，将来源偏好值归属到 policy；原导出路径继续重导出同一类型，架构守卫未放宽。修正仍待 Linux CI 执行确认。
- 第三批开始：实现只读运行模型目录、管理员查询 API 和“模型管理”页面。按模型与真实来源分别展示，支持搜索、来源/适配器/配置状态筛选、分页及来源能力详情；版本以字符串保留精度。
- 目录只读取已发布快照，不查询上游、不修改配置、不发起付费测试。未知能力不推定支持；配置就绪不代表健康、额度、Key 授权或实测成功。未出现在快照的停用渠道不列出。
- 新增 Core 投影、Admin 查询与 API 鉴权/缓存/版本/未知能力回归测试。Windows 缺少 MSVC `link.exe`，本轮后端测试和 clippy 未执行；不得用格式检查或模拟界面验证替代。
- 本轮前端 lint、类型检查、生产构建、Rust 格式检查和 diff 空白检查通过。隔离浏览器验证同名模型分来源、搜索/配置状态筛选、详情未知能力及大整数版本、刷新失败保留与重试恢复；修复分页落入 BaseCard 未渲染插槽的问题并确认分页展示。模拟接口不代表真实后端验证。
- 本轮未提交、未推送、未发布，无数据库迁移或生产数据变更。第三批仅首个只读目录切片，发现快照与差异确认、参考价格版本和指定来源/真实 Key 测试工作台仍待开发；第四至第六批未开始。

下一步：提交/推送经授权后运行 Linux CI，先修复实际失败，再继续第三批发现与差异确认工作流。

## 第二批恢复开发历史检查点（2026-09-11，Asia/Shanghai）

用户已明确要求继续开发。本轮推进第二批剩余分组调度，历史暂停记录保留如下，不再代表当前暂停状态。

- 已核对旧代码提交 `8a3668c` 的 CI 34490405975：最终失败。容器、前端和工作流检查成功；后端在 API 渠道取消测试中得到 `Transport` 而非 `Cancelled`。不是全部验证通过。
- 调整该测试为受控本地 TCP 上游：确认收到字节后触发取消，并保持连接到客户端流结束，移除轮询 MockServer 请求记录与延迟响应的时序依赖。保留取消为 `Ambiguous`、截断响应为 `Sent` 的断言；修正效果仍待后端执行验证。
- 新增迁移 `0010_access_group_routing.sql`：分组满载回退开关、号池/渠道授权关系上的可选优先级和权重覆盖。原迁移 `0001`–`0009` 未修改；校验和追加登记。既有分组默认允许满载回退。
- Core 冻结有效偏好和回退策略；关闭回退时容量拒绝可尝试同级来源，但不进入低优先级。来源/账号容量与故障重试分开，权限、来源限额、共享配额及原生会话锁定保持。
- 管理 API 拒绝未授权/重复/越界覆盖；Store 在配置、关系和审计同一事务保存；管理页面支持独立继承、清空覆盖、满载开关和失败保留输入。
- 已增加 Core 顺序/权重/冻结和协调器容量回退回归、API 非法输入回归、Store 渠道/号池持久化和审计失败回滚回归。前端 lint、类型检查与构建通过；隔离页面验证回填、非法值禁用保存、失败保留与恢复保存。
- 本地定向后端测试在编译依赖时因缺少 `link.exe` 失败，未进入测试；没有安装工具、推送提交或触发新 CI，第二批仍不能标记为完整验收通过。生产服务和生产数据未变；第三至第六批未开始。

下一步：经用户授权提交/推送后运行 Linux CI（含后端全部测试、clippy、迁移及容器门禁），处理实际失败；第二批验收后再进入第三批模型目录与工作台。

## 历史暂停检查点（2026-09-10 22:42，Asia/Shanghai）

用户要求“暂时先开发到这里记录一下”，开发已暂停。保留六批次目标，但必须等用户明确要求继续后再恢复；本次暂停不代表第二批或全部目标完成。

- 工作分支：`feat/gateway-operations`；最新代码提交 `8a3668cf6b44a5c2b8011099c4ad381c7bb11506` 已推送到 fork，见 [草稿 PR #1](https://github.com/y864261947/codex-proxy-rs/pull/1)。暂停记录仅保存并提交到本地，不触发新的 CI。
- 第一批已实现并验证。第二批已实现客户/接入分组/Key 限额、全站准入、渠道管理及 Responses 调用、号池调度、共享配额、来源健康隔离；最新旧 Key 与固定账号探针覆盖修正仍待完整 CI 结论。
- 第二批尚缺分组级优先级/权重覆盖及满载回退策略，这部分只有 [实施设计](source-routing-design.md)，尚未开始代码改动。第三至第六批仍待开发。
- 最新提交修正探针测试缺少真实账号目录的 fixture，并补充不存在账号、Provider 不匹配时必须在调度前拒绝的回归；没有放宽账号权限约束。
- 记录时 [CI 34490405975](https://github.com/y864261947/codex-proxy-rs/actions/runs/34490405975) 仍在运行：前端、工作流检查、冻结迁移检查、Rust 格式和 clippy 已通过；后端测试及容器构建/运行验证尚未完成。后端 job `102915737817`，容器 job `102915737694`。不能将此提交记为完整验证通过。
- 最近完整通过的是提交 `150191b` 的 [CI 34485889558](https://github.com/y864261947/codex-proxy-rs/actions/runs/34485889558)，共 1993 项主测试。后续 `aa78297` 的 [CI 34487915901](https://github.com/y864261947/codex-proxy-rs/actions/runs/34487915901) 后端 2000 项主测试通过，但整个 workflow 被后续推送取消，不能视为完整通过。
- 生产服务与生产数据未更新，本分支功能尚未发布。迁移 `0001` 至 `0009` 已冻结，后续变更新增迁移，不改写已有文件。

用户恢复开发后，先核对上述最新提交的 CI 最终结果并处理实际失败，再完成第二批剩余的分组调度策略及对应验证，然后按第三至第六批顺序推进。后端继续使用 Linux CI；本机缺少 MSVC 链接器，不重复尝试被本地策略阻止的工具安装。暂停期间不继续功能开发或部署。

## 实施顺序与状态

| 批次 | 可验收行为 | 状态 |
| --- | --- | --- |
| 1 | 实时运行请求与最近 60 秒 RPM；首页和监控复用；号池入口与既有功能整合 | 已实现并验证，未发布 |
| 2 | 外部渠道与现有号池进入统一模型来源；下游接入分组与客户/Key 限额 | 分组偏好/满载回退已实现；前端验证通过，新增后端实现及旧 CI 失败修正待 Linux CI，未发布 |
| 3 | 模型目录、发现与价格变更、指定来源/分组测试工作台 | 只读目录、渠道单页手动发现、最新成功记录持久化与草稿确认已实现，后端及迁移待 CI；多版本历史/定时同步/价格/测试工作台待开发 |
| 4 | 按供应商分类的探针、号池巡检、告警与来源状态 | 待开发 |
| 5 | 逐厂商验证协议与图片/视频任务、产物管理、多模态测试 | 待开发 |
| 6 | 自有镜像、迁移演练、版本验收与线上发布 | 待开发 |

批次 1 利用现有数据面先建立实际运行负载的观测能力；后续来源与客户维度复用这套采集，不增加第二套请求记录。未完成批次保持明确状态，不以空页面或静态数值冒充已交付功能。

## 不变量

- 旧 Key 权限范围与空池拒绝语义保持；流式交付后的请求不得换号重放。
- 数据库变更新增迁移，原有迁移 checksum 不变。
- 运行占用与完成日志分开；一次逻辑请求的多次上游尝试不能增加下游并发。
- 测试与探针不混入客户业务统计，但仍受真实来源容量约束。
- 生产服务和生产数据不作为开发测试环境。

## 验证记录

本机前端 lint、类型检查、生产构建与 Rust 格式检查通过。Windows 缺少 MSVC 链接器，安装构建工具被本地执行策略阻止；后端已改用分支 PR 的 Linux CI 完成验证。

已建立 [草稿 PR #1](https://github.com/y864261947/codex-proxy-rs/pull/1)，并启用 fork 原本默认停用的 Actions。[代码提交 3450dbf 的 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34465417956) 已完成以下功能检查：

- Rust clippy（所有目标、所有特性、警告视为错误）。
- 9 个 crate 的 1903 项主测试全部通过，包含 PostgreSQL/Redis 集成测试；新计数器过期、并发记录、准备取消、会话释放、HTTP 入口排除、管理员鉴权和 WebSocket 心跳排除均通过。
- 前端 lint、类型检查与生产构建；工作流 lint；冻结迁移检查。
- Linux 容器镜像构建和现有运行镜像验证。

首轮新增 API 测试曾因模拟管理 Key 未满足既有长度规则而失败，已修正测试数据并重跑通过；没有放宽鉴权规则。上述验证记录覆盖批次 1，后续改动另行记录。

本机使用独立模拟 API 做浏览器视觉与交互检查：验证首页、监控中心、旧账号/分组地址跳转、号池分区切换，以及采样失败保留旧值和首次失败显示未知值。模拟数据仅在忽略目录 `.runtime` 内，用于 UI 检查，不代表真实业务流量或后端联调通过。

## 批次 1 的实际行为

- `GET /api/admin/dashboard/realtime` 复用管理端鉴权与 `no-store`，直接读取共享内存计数；每 5 秒轮询不增加数据库查询。
- 入口 RPM 统计四个现有业务 POST 端点和 WebSocket 文本创建尝试，包括未通过认证/解码的请求；后台查询、模型列表、连接升级、空闲连接及内部账号探针排除。采用当前秒及之前 59 秒的固定桶，精度 1 秒。
- 实时并发是进入 Core 的逻辑执行数，拆分为准备中和执行中。重试不重复累计；失败、正常终态、取消收敛或会话丢弃释放计数。短暂的认证/解码阶段不计入执行并发。
- 指标仅覆盖当前单进程；重启清零，启动不足 60 秒和采样失败在 UI 明示。尚未引入队列和异步视频任务，不显示虚构的零值。
- 首页与监控中心复用实时负载卡片；监控中心同时展示既有号池容量和健康趋势（这些完成统计保持原有 30 秒刷新）。
- 自建号池下提供账号、号池分组两个入口；旧 `/accounts`、`/account-groups` 地址继续跳转，具名路由保留。
- 本批次未修改数据库迁移、下游 Key 权限和生产部署。

## 批次 2 开发记录

- 准入从单 Key 扩展为同一请求的多个独立限额范围；Redis 单次原子检查全部通过后才同时占用，并按请求 ID 幂等去重。
- 释放队列保存本次请求冻结的全部范围，客户归属后续变化不会误释放新客户的占用；RPM 记录在并发释放后保留。
- 已补充多个 Key 竞争同一客户容量、子级/父级拒绝无部分占用、重复请求与重复释放的测试；提交 `ace046a` 的 [CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34467429460) 全部通过，9 个 crate 共 1909 项主测试通过。
- 客户管理、持久化归属、统一来源与接入分组仍在开发，不能视为第二批完成。
- 新增 `0002_customers.sql`：客户及 Key 归属、历史请求客户引用；禁止删除仍有关联 Key 的客户。快照读取客户启用状态与独立限额，历史恢复不依赖当前客户/Key 行。
- 客户禁用认证、快照冻结、两种请求落库路径与改归属/删除后的恢复测试已编写。首轮数据库测试中两个新增 fixture 漏写 Key 表必填时间戳，已补齐；没有修改原表约束，修正后的完整版本等待 CI 验证。
- 新增客户管理端口、服务和管理员接口，创建/编辑/删除/Key 归属更新与配置版本和安全审计原子提交；冲突回滚版本与审计，关联客户名称和状态进入 Key 列表投影。
- 下游接入下提供 API 密钥、客户两个分区；客户页管理共享限额和启停，Key 列表独立设置归属。客户查询与选择提供搜索/分页/加载失败反馈，归属修改要求明确选择客户或解除归属。
- 独立模拟 API 的浏览器检查覆盖客户创建、4 并发/60 RPM 保存、搜索后保留选择、归属更新、客户停用、加载失败禁止保存及重试恢复。发现独立客户列被固定操作区遮挡，已改为名称下入口；客户停用显示为 Key 状态。该检查不替代真实后端集成测试。
- 客户关联后的 PostgreSQL 回归测试发现 Key 游标排序及搜索条件存在未限定列名，已统一使用 Key 表别名；`ON DELETE RESTRICT` 返回的 `23001` 已映射为冲突，保证失败操作回滚版本、审计与归属。
- 接入分组核心策略与 `0003_access_groups.sql` 已实现：独立组 ID、精确模型白名单、显式号池集合、组并发/RPM，模型列表与执行授权共用策略。空模型/空号池不退化为全部权限；未关联接入分组的旧 Key 保留原语义。
- 请求在模型别名映射之前检查对外模型权限；使用接入分组的 Key 调用 Provider 原生端点时，必须提供由 Provider 识别的已授权模型，无模型请求拒绝，旧 Key 的原有原生端点行为不变。
- 组归属冻结在请求历史与释放范围内，重启恢复从历史读取。新增快照隔离、模型别名授权、空池拒绝、禁用组、改组/删除后恢复和双落库路径测试。接入分组管理接口/页面及渠道来源尚未完成，此阶段等待 CI 验证。
- 已接入接入分组的管理服务与 `/api/admin/access-groups` 列表、创建、编辑、删除、归属接口；权限、池关系、版本和审计事务提交。非法号池、重复名称、关联删除与无效归属的回滚测试已补充，等待 CI；管理页面与 Key 归属展示继续开发。
- 接入分组页面及 Key 归属入口已实现；新 Key 在一次创建事务中保存客户和接入分组，关联不存在时整个创建回滚。Key 列表保留旧账号分组信息，但供应商展示按有效接入分组权限计算，空池不显示全站供应商。
- 接入分组独立浏览器检查通过：创建两模型/单号池/4 并发/60 RPM 的分组、Key 同时绑定客户和组、搜索后保留已选归属、加载失败禁止提交及重试恢复、旧 Key 改组和组停用状态。已修复复合表单控件重复 ID；前端 lint、类型检查和构建通过。模拟检查未访问生产数据，不替代后端联调。
- 提交 `f25addd` 的 CI 通过 clippy、容器运行验证和前端检查；后端仅两处新增测试数据因颜色缺少透明度字节而失败，已按既有约束修正为 8 位十六进制颜色。修正及原子 Key 创建已推送为 `d2db8bd`，完整 CI 等待结果。
- 客户、接入分组与原子 Key 创建的最终提交 `e5ddb45` [CI 全部通过](https://github.com/y864261947/codex-proxy-rs/actions/runs/34472447418)：9 个 crate 共 1929 项主测试、clippy、前端检查、冻结迁移与容器运行验证通过。包含权限/快照、数据库事务回滚、恢复、多层 Redis 准入和新增接口验证。
- 新增全站并发/准入 RPM 硬限额及 `0004_global_admission.sql`，管理端提供独立配置和审计事务，旧运行设置保存不覆盖新限额。全局范围始终计数，配置从不限改为有上限时包含已经运行的请求；全站容量拒绝与下游自身限流区分。
- 全站恢复聚合业务请求并排除内部探针；新增全局争用、降低上限、拒绝不占用、释放保留 RPM、快照验证、审计失败回滚与旧设置兼容测试，等待 CI。WebSocket 每次新生成重新冻结当前权限/限额，防止长连接沿用停用前的客户、分组或全站配置。
- 全站限额前端 lint、类型检查、构建和独立浏览器检查通过：12 并发/500 RPM 保存、失败保留旧值并禁止提交、首次读取失败为空值、重试恢复。真实后端行为以本批 CI 为准，生产尚未发布。
- 全站限额提交 `f2f9e2f` 的 CI 后端、前端与工作流检查通过；容器构建仍在运行。API 缓存测试已改用完整管理路由，覆盖实际挂载的 `no-store` 中间件。
- 提交 `f2f9e2f` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34473699614) 已通过：9 个 crate 共 1936 项主测试，前端检查、clippy、冻结迁移和容器运行验证通过。全站限额与长连接策略刷新已验证，未发布。
- 渠道来源基础契约已实现：稳定渠道/共享配额 ID、独立来源策略、显式允许集合，以及账号/渠道两种真实调用目标。候选必须匹配具体渠道，号池候选必须确认实际账号的池归属与 Provider；渠道不生成账号反馈或账号续接钉选。已补隔离和租约释放回归测试，等待 CI。实际渠道配置和调用尚未接通，设计见 [来源路由实施设计](source-routing-design.md)。
- 来源调用目标提交 `e009761` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34474477264) 已通过，9 个 crate 共 1942 项主测试、clippy、前端与容器运行验证通过。
- 渠道目录已按具体渠道 ID 保存模型能力；同名模型跨渠道不合并，缺少启用策略、权限或对应能力时拒绝，旧账号路径不获得渠道目录权限。该能力尚未接入实际渠道配置与执行，等待本轮 CI。
- 新增 `0005_source_history.sql`：请求及中间失败保存来源类型、稳定引用和名称快照；渠道不需要虚构账号。增加显式无账号权限的历史范围，保留原有账号约束与发送水位。日志列表、详情与尝试记录透传来源；来源改名不影响既有历史。已补双写入路径、重试来源、NULL 约束绕过、无账号渠道投影和 API 回归测试，等待 CI。
- 来源日志的独立浏览器检查已覆盖渠道、号池和旧账号三种列表展示、请求详情、重试前后来源与错误详情；修正渠道详情头部显示空账号的呈现。前端类型检查、lint 和构建通过，模拟数据不代表实际渠道调用已经接通。
- `300a80e` 通过 clippy 和前端检查；Core 回归中一处旧断言仍把空账号权限记为 `groups`，已更新为本批新增的 `none`，同时保留空权限拒绝和已选号池仍为 `groups` 的断言。完整后端回归需重跑，尚不能算本批验证完成。
- 来源目录与历史日志提交 `5d4602a` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34475905651) 全部通过：9 个 crate 共 1948 项主测试、clippy、前端检查、冻结迁移和容器运行验证通过。
- 接入分组的执行入口已接入号池来源计划：候选按优先级和同级权重冻结顺序，再将账号选择限制到该池与原请求权限的交集；旧 Key 保持原路径。号池名称、启停进入运行快照，实际来源进入已有请求历史。池容量和偏好配置尚待接通，当前编译使用默认偏好和不限额。
- 原生会话增加具体号池绑定，Redis 保存来源并兼容无来源的旧记录；非法来源拒绝恢复。即使账号同时属于两个池，续接及 Provider 允许的恢复也不会跨越原池。新增候选隔离、权重分布、执行上下文、会话来源和 Redis 回归测试，等待本次 CI。
- 号池调度首轮 CI 发现会话状态导致枚举体积超过项目 lint 阈值，已改为共享不可变状态；同时移除新测试中一个冗余克隆。提交 `54b1416` 正在重新验证，不放宽检查规则。
- 渠道配置持久化基础已实现：新增 `0006_channels.sql`、不透明 Provider 连接配置、公共列表投影、带版本条件的更新/删除、配置与安全审计原子提交、Provider 专用凭据读取端口。已补轮换、停用、旧版本拒绝、审计失败回滚和脱敏测试，等待 CI；管理用例、HTTP/UI、运行快照和具体协议适配仍待接通，此记录不代表渠道已经可调用。
- 号池调度提交 `54b1416` 的后端 clippy 与 1954 项主测试、前端及工作流检查通过；容器运行验证在后续推送时被取消。渠道配置基础提交 `09868eb` 已推送，完整 CI 继续覆盖上述改动。
- 首个 Responses API 渠道的 Provider 配置解析已实现：独立 Base URL/API Key/组织与项目头/显式模型集合，拒绝 URL 内嵌凭据、查询密钥和头注入；公共投影无完整 Key，编辑省略 Key 时保留旧值。使用现有 OpenAI crate 的独立 `api` 模块，实际数据面和管理入口继续接通，尚未声明调用能力。
- `09868eb` 的新渠道轮换、脱敏和事务回滚测试均通过，clippy 与前端检查通过；数据库初始化回归仅因预期表清单未加入 `upstream_channels` 而失败，已补齐清单并重跑，未修改已冻结的迁移。
- `fa15599` 的 clippy 与前端检查通过；架构回归发现 OpenAI 新增 `api` 模块未加入显式公开清单，已补齐该模块，保留其余边界检查。
- 渠道管理用例、Provider 独立配置注册表与 `/api/admin/channels` 接口已接通应用初始化。创建、带版本编辑/删除、列表、脱敏连接详情和适配器列表均需管理员权限并禁止缓存；版本以字符串传输，避免浏览器整数精度丢失。新增 Provider 拒绝、旧版本拒绝、提交失败不发布、公共投影与接口鉴权回归，等待 CI。前端和实际数据面继续开发。
- 渠道页面已接通列表、创建、编辑、删除和版本校验；支持 Responses 连接、显式模型集合及调度/容量配置。独立模拟 API 浏览器检查通过新增渠道、保留 Key 修改为 4 并发/60 RPM、读取失败禁用保存、重试恢复及并行修改冲突拒绝；前端 lint、类型检查和构建通过。实际渠道路由与限额执行继续接通，尚未发布。
- 渠道管理提交 `c9dda61` 的 clippy 及 Admin 101 项测试通过；API 新增测试缺少真实应用入口会注入的请求 ID，且源码/测试显式清单漏加渠道文件，已补齐测试接入条件并重跑，未放宽管理员鉴权。
- 渠道公共策略和连接版本已进入数据库一致性快照，凭据不进入 Core 快照。目录、候选和 Provider 调用事实携带同一渠道版本；拒绝将不同版本目录合并，拒绝旧候选确认新版本调用。新增数据库快照隔离、目录与配置竞争、Provider 不匹配和无版本调用拒绝测试，等待 CI；下游渠道授权与实际 HTTP 调用尚待接通。
- 渠道管理与控制台提交 `6380c51` 的后端 clippy、9 个 crate 共 1965 项主测试、前端与工作流检查全部通过；容器验证仍在运行。配置版本进入路由的后续提交 `ffec632` 尚待单独验证。
- 提交 `6380c51` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34479782834) 已通过，包含容器镜像构建与运行验证；渠道版本冻结的后续提交已进入下一轮 CI。
- 接入分组增加显式渠道集合，新增 `0007_access_group_channels.sql`，空集合不回填任何权限。配置更新、来源关系、版本与审计事务提交；渠道有关联分组时禁止删除。模型列表、模型资料与模型校验合并授权渠道目录，再应用对外模型白名单；Key 供应商展示纳入启用渠道。新增空来源、旧 Key、停用渠道、模型别名、关系回滚和权限快照测试，等待 CI。
- 接入分组提供渠道搜索/分页勾选，跨搜索保留选择；渠道目录加载失败时禁止保存，重试恢复后可提交。独立浏览器检查覆盖无号池的渠道组保存、搜索无结果保留原授权及读取失败恢复；前端 lint、类型检查和构建通过。实际 Provider HTTP 调用与渠道容量执行仍在开发。
- 渠道版本冻结提交 `be6485b` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34480729421) 已通过：9 个 crate 共 1968 项主测试、clippy、前端、冻结迁移和容器运行验证通过；接入分组渠道授权进入后续独立验证。
- 接入分组授权提交 `c94ed5d` 通过 clippy 与前端检查，后端在架构检查处因 `policy/access.rs` 新增中立渠道 ID 依赖未加入清单而失败；已仅为该文件补齐 `identity`，继续禁止依赖执行和路由层。
- `openai_api` 渠道 Provider 已接入应用组合根，按候选冻结版本读取凭据，返回延迟到首次 poll 才发送的 Responses SSE 流；不进入 OAuth 选号。保留模型映射、参数与原始 SSE，记录用量和首字等时延，第三方渠道暂不继承 OAuth 价格估算。配置轮换/停用、重定向、请求头隔离、截断与取消回归已编写，等待 CI。渠道级续接和后台异步任务尚未实现，明确拒绝相关请求；渠道/号池容量仍在开发。
- `31a4fa1` 的 clippy、分组渠道授权、数据库集成与前端检查通过。新增 HTTP 测试的模拟库把 SSE Content-Type 覆盖为 text/plain，导致成功流用例被正确拒绝；已改为显式 MIME 的原始响应体，并在截断用例中断言先收到 wire 事件，避免只验证到 Content-Type 拒绝。实际转发等待修正后的完整 CI。
- 新增 `0008_pool_source_controls.sql` 与号池来源调度/容量配置：默认保持原来不限额、同级等权重行为；配置可随创建/编辑原子提交，省略来源配置的描述编辑不覆盖限额。快照冻结号池偏好、限额与共享配额引用，号池编辑界面复用原入口。新增数据库审计失败回滚、旧快照保留和 HTTP 参数校验测试，等待 CI；实际容量占用仍待接通。
- 号池配置提交 `929091c` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34482937989) 通过：9 个 crate 共 1980 项主测试、clippy、前端、冻结迁移及容器运行验证通过。独立浏览器检查覆盖已有配置回填、保存后重开保留、服务失败保留输入和恢复后保存。
- 来源容量已接入 Core 调用前边界：快照冻结限额，Store 复用 Redis 原子多范围计数器，号池/渠道以独立来源身份累计，每次尝试使用唯一租约 ID。满载在允许重试的边界切换候选，原生会话不跨来源；基础设施失败不发送上游请求，不计入上游故障熔断。Host 管理有界协调 worker，等待者取消后仍清理后到的 grant；流 EOF、取消及会话丢弃释放容量，保留调用事实供交付。新增争用、降低限额、共享配额原子性、迟到取消与租约释放回归，等待 CI。
- 共享配额的原子计数端口已建立，配置实体与快照解析尚待接通；此开发阶段带未解析共享配额 ID 的候选拒绝调用，不能把共享配额当作不限额。来源健康隔离、共享配额管理等仍属于未完成的第二批，本轮未部署生产。
- 来源租约提交 `5cef773` 的后端 clippy、9 个 crate 共 1987 项主测试、前端与工作流检查通过；容器运行验证仍在进行。新增测试覆盖真实 Redis 争用和跨渠道共享配额、取消等待后的迟到准入清理、满载切换来源、流 EOF/取消/丢弃时释放、原生会话不能跨来源绕过容量。
- 新增 `0009_shared_quota_scopes.sql`：具名共享配额及来源外键，早期只有 ID 的引用迁移为停用配置，已关联的配额禁止删除。配额启停和限额进入同一数据库一致性快照，并冻结到号池/渠道候选；缺失或停用配额拒绝调用。增加引用完整性、旧快照保留和渠道候选配置隔离测试，等待 CI；管理用例、接口与选择页面继续开发。
- 来源租约提交 `5cef773` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34484192196) 已通过，包括容器运行验证。共享配额快照提交 `72dc22d` 在迁移检查处因新迁移遗漏校验和而停止，已验证 `0001`–`0008` 未改动并追加 `0009` 校验和，等待重跑；未修改原有迁移或放宽检查。
- 共享配额管理用例、Store 事务与 `/api/admin/quota-scopes` 查询/创建/修改/删除接口已接通，来源引用数量覆盖渠道及号池，配额字段、版本和审计原子提交。新增管理员鉴权、参数拒绝、名称冲突、引用删除拒绝、审计失败回滚及快照更新回归，等待 CI；前端管理与选择控件继续开发。
- `a63e5b2` 的迁移冻结及前端检查通过；后端 clippy 因新增快照测试中一个冗余克隆而停止，已移除该克隆，未放宽检查规则。共享配额配置和管理的完整后端回归等待修正后的 CI。
- 上游渠道内新增“共享配额”分区；渠道、号池复用搜索/分页选择器，跨搜索保留关联，停用配额明示阻止新请求，目录读取失败保留输入并禁止保存。独立浏览器检查通过创建 3 并发/45 RPM 配额、渠道与号池共用同一配额、搜索无结果保留选择、失败后重试保存和停用提示；前端 lint、类型检查及生产构建通过。该检查使用本地模拟 API，未访问生产。
- 共享配额完整代码提交 `150191b` 的 [CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34485889558) 已通过后端 clippy、9 个 crate 共 1993 项主测试、前端检查、工作流与冻结迁移检查；容器运行验证仍在进行。来源健康隔离、旧 Key/固定账号诊断的来源准入覆盖等第二批验收项尚未完成，见来源路由设计。
- 共享配额提交 `150191b` 的 [完整 CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34485889558) 已全部通过，包含容器构建与运行验证；9 个 crate 共 1993 项主测试通过，生产未发布。
- 来源健康范围已接入 Core 路由与尝试反馈，渠道/号池分别累计熔断，共享配额不共享健康状态；无来源的旧路径保留 Provider 范围。所有读取共用 100 ms 预算并限制并行数，失败保持可重建状态 fail-open。Store 保留原 Redis 状态机及有界反馈队列，新增真实 Redis 隔离、渠道故障与恢复、原生会话禁止跨池、未授权来源不兜底和本地错误不计故障回归，等待 CI。旧 Key 与固定账号诊断的来源准入覆盖继续开发。
- 来源健康提交 `136c19d` 的 CI 因新增测试中两处多余策略字段及一处未补默认值的路由上下文而停止；已在 `aa78297` 修正，未更改生产权限或放宽检查。修正后的 clippy 与前端检查通过，完整测试及容器验证仍在运行。
- 旧 Key、Provider 自有端点与固定账号诊断已接入号池来源计划。账号交集保持原权限/历史，只对真正未分池账号提供无来源后备；停用、缺失或满载号池不能通过旧入口绕过。固定账号诊断也占用实际号池，来源容量错误归为网关。未分池索引随快照复用，新增权限交集、候选范围和执行准入回归，等待 CI。
- 来源健康修正提交 `aa78297` 的 [CI](https://github.com/y864261947/codex-proxy-rs/actions/runs/34487915901) 后端 clippy、9 个 crate 共 2000 项主测试及前端检查通过，容器运行验证尚待最终状态确认；旧入口覆盖提交 `90f1bd4` 已推送进入独立验证。第二批剩余分组偏好与满载回退验收见来源路由设计。
- 旧入口覆盖提交 `90f1bd4` 的 clippy 因测试中的冗余克隆停止，已在 `950f0d4` 修正。后者 clippy 通过，回归发现诊断的显式 Provider 会绕过原路由中的空账号范围检查，导致生成无来源后备候选。已将显式 Provider 改为账号权限内的过滤条件，并补充空范围、跨 Provider 与停用池诊断拒绝用例，等待修正后的完整 CI。未放宽测试或来源容量约束。
- 已同步公开 API 与架构文档，补齐客户、接入分组、渠道、共享配额、全站准入及实时视图的源码合同，并区分实际部署状态。
- `a471369` 的 clippy 与新号池准入/诊断回归通过，三个旧探针错误测试因 fixture 从未提供目标账号目录而被更早拒绝。已补齐既有 `acct_probe` 的目录事实，保留原上游/Provider 错误断言，并增加缺失账号和 Provider 不匹配在发送前拒绝的测试。完整工作区回归继续验证，未恢复空范围旁路。
